<section id="contact" class="col-6 col-12-narrower">
    <header>
        <h2></h2>
        <h2>Recommendations for the audit</h2>
        <p>Trigger this analysis to consider and find out through the CUBE
            how other nearby audits can help to make better use of current resources, heading you to know:
            Where are you? Opportunities, risks and threats to look for! How to get there? (and, potentially, obtain the desired outcome)</li>
        </p>
        <p>Such analysis can be performed at the planning or the execution phases of the audit.</p>
    </header>
    <% if(msg != ''){ %>
        <div class="col-12" id="save_auditplan">
            <p>
                <span class="successformtext"><%= msg %></span><br/>
            </p>
        </div>
    <% } %>    
   <form method="POST" action="/analytics/Recommendations">
        <div class="row gtr-50">
            <p>
                <% if(Matrix.length > 0) { %>
                    <div class="col-12">
                        <ul class="actions">
                            <% if(Matrix.length > 0) { %>
                                <li><input type="submit" class="button alt" value="Get recommendations" /></li>
                            <% } %>
                        </ul>
                    </div>
                    <p></p>
                    (The ten most relevant terms discovered and used in similarity analysis are presented)<br/>
                    <table id='tablecatalog'>
                        <thead>
                            <% include ./recommendationheader %>
                        </thead>    
                        <tbody>
                            <% include ./recommendationrow %>
                        </tbody>
                    </table>
                    <input type="hidden" name="rows_count" id="rows_count" value="<%= Matrix.length %>"/> 
                <% } else { %>
                    <p>No terms discovered for analysis!</p>
                <% } %>
            </p>
        </div>
    </form>
</section>
